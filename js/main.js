function gbi(id) {
    return document.getElementById(id);
}

function makeLink(obj) {
    return '<a href="./' + obj.file + '"';
}

let nav = gbi('nav');

window.onload(function() {
    nav.innerHTML += 
});